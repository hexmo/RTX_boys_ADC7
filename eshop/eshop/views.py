from django.http import HttpResponse
from django.shortcuts import render

#displays the message when directly runnning the server
# def HomeMessage(request):
#     return HttpResponse("this is the home page")